## TODO List

- [x] 定义`task_t` 结构
- [ ] `utils` / Build建立 `Makefile` 和基础的 **`log`** 与 **`color`** 模块，确保编译和调试环境
- [ ] `database`实现数据库初始化 (`db_init`) 和**核心 CRUD (Create)** 操作。
- [ ] `cli` / `parser`建立 CLI 主循环，实现最简单的 `add "Task title"` 命令。

## General Naming Conventions

![[GeneralNamingConventions]]

---

## Modules Design

![[ModulesDesign]]

---

## Data Types

![[DataTypes]]